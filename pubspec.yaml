name: contacts
description: A set of classes useful for managing contacts. In order to save data in a database, each class has `toMap()` method, and to retrieve data from the database, each class has `fromMap(Map<String, dynamic> map)`.
version: 0.2.0-dev.1
homepage: https://marco87.dev

environment:
  sdk: ">=2.12.0-29.10.beta <3.0.0"
  flutter: ">=1.24.0-10.2.pre <2.0.0"

dependencies:
  flutter:
    sdk: flutter

  pedantic: ^1.10.0-nullsafety.3
  url_launcher: ^6.0.0-nullsafety.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
